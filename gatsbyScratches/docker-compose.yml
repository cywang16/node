version: '3'
services:
  gatsbyscratches:
    image: node
    # command: sh -c "while true; do echo 'Pause'; sleep 10; done"
    command: node /idle/wait.js /idle/index.html
    ports:
     - "7080:8080"
     - "8080:8000"
    volumes:
     - .:/code
     - ../utilities:/idle
